module top(
    input a,
    input b,
    input c,
    output reg out
);
    // Write your code here
    always@(*)
        begin
            if(a==1 &&b==1)
                out=1;
            else if(b==1 && c==1)
                out=1;
            else
                out=0;
        end
endmodule
