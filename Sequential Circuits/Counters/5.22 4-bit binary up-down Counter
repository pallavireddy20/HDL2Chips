module top(
    input clk,
    input up,
    output reg [3:0] q
);
    // Write your code here
    always @(posedge clk)
        begin
            if(up)
                q<=q+1;
            else
                q<=q-1;
        end
endmodule
--------------------------------------
