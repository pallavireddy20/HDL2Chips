module top(
    input clk,
    input en,
    input rst,
    output reg [3:0] q
);
    // Write your code here
    always @(posedge clk) begin
    if (rst)
        q <= 4'd0;
    else if (en)
        q <= q + 4'd1;
end
endmodule
