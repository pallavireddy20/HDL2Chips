module top(
    input clk,
    input enable,
    input d,
    output reg q
);
    // Write your code here
    initial q=1'b0;
    always@(posedge clk)
        begin
            if(enable)
                q<=d;
        end
endmodule
