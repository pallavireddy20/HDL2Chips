module top(
    input s,
    input r,
    output reg q
);
    // Write your code here
    always@(*)
        begin
            if(s)
                q<=1;
            else if(r)
                q<=0;
        end
endmodule
