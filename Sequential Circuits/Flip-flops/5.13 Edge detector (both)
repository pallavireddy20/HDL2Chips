module top(
    input clk,
    input in,
    output out
);
    reg in_d1 = 0;
    reg in_d2 = 0;

    always @(posedge clk) begin
        in_d1 <= in;
        in_d2 <= in_d1;
    end

    assign out = in_d1 ^ in_d2;

endmodule
