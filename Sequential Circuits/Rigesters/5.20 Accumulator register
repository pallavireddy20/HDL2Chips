module top(
    input clk,
    input reset,
    input [7:0] d,
    output reg [7:0] q
);
    // Write your code here
    always @(posedge clk)
        begin
          if (reset)
        q <= 8'b0;      // synchronous reset
    else
        q <= q + d;
        end
endmodule
