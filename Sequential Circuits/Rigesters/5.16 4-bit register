module top(
    input clk,
    input [3:0] d,
    output reg [3:0] q
);
    // Write your code here
    always @(posedge clk)
        begin
            if(clk)
                q<=d;
        end
endmodule
