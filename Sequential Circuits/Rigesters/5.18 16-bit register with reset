module top(
    input clk,
    input rst,
    input [15:0] d,
    output reg [15:0] q
);
    // Write your code here
    always @(posedge clk)
        begin
            if(rst)
                q<=0;
            else
                q<=d;
        end
endmodule
