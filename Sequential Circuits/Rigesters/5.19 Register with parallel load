module top(
    input clk,
    input load,
    input [7:0] d,
    output reg [7:0] q
);
    // Write your code here
    always @(posedge clk)
        begin
            if(load)
                q<=d;
        end
endmodule
