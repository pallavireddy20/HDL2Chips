module top(
    input clk,
    input en,
    input [7:0] d,
    output reg [7:0] q
);
    // Write your code here
    always@(posedge clk)
        begin
            if(en)
                q<=d;
           
        end
    
endmodule
